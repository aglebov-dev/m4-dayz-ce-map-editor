# Рантайм-зависимости редактора (для запуска из исходников и для сборки).
# Сборочный инструмент (Nuitka) ставит pyside6-deploy сам — см. src/pysidedeploy.spec.
PySide6==6.11.1
numpy==2.5.1
pillow==12.3.0
paramiko==5.0.0
